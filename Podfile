#
ENV['COCOAPODS_DISABLE_STATS'] = "true"
#
platform :ios, '8.1'
use_frameworks!

target 'TestCocoaPod' do
  pod 'MBProgressHUD', '~> 0.9.0'

  target 'TestCocoaPodTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
